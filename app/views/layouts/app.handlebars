<!DOCTYPE html>
<html lang="en">
	<head>
		{{> head}}
	</head>
	<body>
		<div class="container">	
			{{> header}}
		</div>
		
		{{{body}}}
		
		<div class="container">	
			{{> footer}}
		</div>
		
		{{> scripts}}
	</body>
</html>	